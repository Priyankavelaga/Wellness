# Wellness

>> pip install -r requirements.txt
or
>> pip install flask openai
>> pip install markdown
>> pip install requests
>> pip install requests beautifulsoup4

>> export OPENAI_API_KEY="your_openai_api_key"
>> export API_KEY="your-google-api-key"
>> export CSE_ID="your-google-cse-id"


Google Search Engine > Create new engine > Name the engine > What to search (https://www.yogajournal.com) > Add > Enable Image Search > Create
Google cloud Console > Enable Custom Search API > Create Credentials > Create API key 

